import React from 'react';
import { Main } from '.';

const Wrapper = (): React.ReactElement => {
  // const keypressHandler = (ev: KeyboardEvent) => {
  //   if (ev.key === 'n') {
  //     setInit(true);
  //   }
  // };

  // useKeybordInput(keypressHandler);

  return (
    <>
      <Main />
    </>
  );
};

export { Wrapper };
